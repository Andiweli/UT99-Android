plugins {
    // API 35 requires Android Gradle Plugin 8.6.0 or newer.
    // 8.6.1 is used here as the stable API-35-compatible baseline.
    id 'com.android.application' version '8.6.1' apply false
}
